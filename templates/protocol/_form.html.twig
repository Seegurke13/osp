{{ form_start(form) }}
    <div id="template-protocolContent" class="template">
        {{ form_widget(form.protocolContent.vars.prototype) | raw }}
    </div>
    <div id="template-tag" class="template">
        {{ form_widget(form.tags.vars.prototype) | raw }}
    </div>
    <div id="template-participants" class="template">
        {{ form_widget(form.participants.vars.prototype) | raw }}
    </div>

    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
