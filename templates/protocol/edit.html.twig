{% extends 'page.html.twig' %}

{#{% block form_row %}#}

    {#<div class="form-group {{ form.vars.attr.class }}">#}
        {#{{ form_label(form) }}#}
        {#{{ form_errors(form) }}#}
        {#{{ form_widget(form) }}#}
    {#</div>#}

{#{% endblock %}#}

{% block title %}Edit Protocol{% endblock %}

{% block content %}
    <h1>Edit Protocol</h1>

    {{ include('protocol/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('protocol_index') }}">back to list</a>
{% endblock %}
